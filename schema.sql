CREATE TABLE county_data(
	state VARCHAR,
    county VARCHAR,
    year INTEGER,
    property_type VARCHAR,
    inventory INTEGER,
    homes_sold INTEGER,
    median_sale_price DECIMAL,
    median_ppsf DECIMAL
);