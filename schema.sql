CREATE TABLE county_data(
	period_end DATE,
    region VARCHAR,
    parent_metro_region VARCHAR,
    state VARCHAR,
    property_type VARCHAR,
    inventory INTEGER,
    homes_sold INTEGER,
    median_sale_price DECIMAL,
    median_ppsf DECIMAL
);